#include "main.h"

/**
* findFilePath - looks for files in the current PATH
* @filename: filename to look for
* Return: path of the file if found, NULL otherwise
*/
void findFilePath(const char *filename)
{
    char *path = getenv("PATH");

}
